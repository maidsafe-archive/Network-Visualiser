<% if(user.enabled) { %>
<button ng-click="clearPendingSessions()">Clear Pending Sessions</button>
<% } %>
<label class="counter">Session Count - {{activeSessions.length ? activeSessions.length : 0}}</label>